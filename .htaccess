<IfModule mod_rewrite.c>
  RewriteEngine on
  # Do not rewrite if the request points to a real file or directory
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ index.php/$1 [L]
  
</IfModule>